import { default as Root } from './root.svelte';
import { default as Viewport} from './viewport.svelte';
import { default as Content} from './content.svelte';
import { default as Scrollbar } from './scrollbar.svelte';
import { default as Thumb } from './thumb.svelte';
import { default as Corner } from './corner.svelte';

const ScrollArea = {
    Root,
    Viewport,
    Content,
    Scrollbar,
    Thumb,
    Corner
};

export default ScrollArea;