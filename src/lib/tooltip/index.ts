import { default as Provider } from './TooltipProvider.svelte';
import { default as Root } from './Tooltips.svelte';
import { default as Trigger } from './TooltipTrigger.svelte';
import { default as Content } from './TooltipContent.svelte';

const Tooltip = {
    Provider,
    Root,
    Trigger,
    Content,
};

export default Tooltip;

